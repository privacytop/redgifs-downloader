import Stub from './Stub'

/** Placeholder — replaced by the Following page workflow. */
export default function Following(): JSX.Element {
  return <Stub title="Following" kicker="following" kickerIndex={3} />
}
