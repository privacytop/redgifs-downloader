import Stub from './Stub'

/** Placeholder — replaced by the Collection detail page workflow. */
export default function CollectionDetail({
  id,
  title
}: {
  id: string
  title: string
}): JSX.Element {
  void id
  return <Stub title={title} kicker="collection" />
}
