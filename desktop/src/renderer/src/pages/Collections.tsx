import Stub from './Stub'

/** Placeholder — replaced by the Collections page workflow. */
export default function Collections(): JSX.Element {
  return <Stub title="Collections" kicker="library" kickerIndex={5} />
}
