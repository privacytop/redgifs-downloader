import Stub from './Stub'

/** Placeholder — replaced by the Discover page workflow. */
export default function Discover(): JSX.Element {
  return <Stub title="Discover" kicker="discover" kickerIndex={2} />
}
