import Stub from './Stub'

/** Placeholder — replaced by the Niche detail page workflow. */
export default function NicheDetail({ id, title }: { id: string; title: string }): JSX.Element {
  void id
  return <Stub title={title} kicker="niche" />
}
