import Stub from './Stub'

/** Placeholder — replaced by the Creator page workflow. */
export default function Creator({ username }: { username: string }): JSX.Element {
  return <Stub title={`@${username}`} kicker="creator" />
}
